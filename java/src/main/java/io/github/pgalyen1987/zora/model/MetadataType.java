// Generated by codegen/emit_java.py from Zora's OpenAPI spec. Do not edit.
package io.github.pgalyen1987.zora.model;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Arrays;
import java.util.Collections;
import java.util.List;
import java.util.Objects;

/**
 * How coin metadata is supplied. Only RAW_URI is supported. An extensible enum: values Zora adds later
 * are kept, not rejected.
 */
public final class MetadataType {
    /** {@code RAW_URI} */
    public static final MetadataType RAW_URI = new MetadataType("RAW_URI");

    /** Every value this SDK knows. */
    public static final List<MetadataType> ALL = Collections.unmodifiableList(Arrays.asList(RAW_URI));

    private final String value;

    private MetadataType(String value) {
        this.value = Objects.requireNonNull(value);
    }

    /** Wraps any value, including ones this SDK doesn't know. */
    @JsonCreator
    public static MetadataType of(String value) {
        for (MetadataType known : ALL) if (known.value.equals(value)) return known;
        return new MetadataType(value);
    }

    /** The value as the API spells it. */
    @JsonValue
    public String value() {
        return value;
    }

    @Override
    public boolean equals(Object o) {
        return o instanceof MetadataType && (((MetadataType) o).value).equals(value);
    }

    @Override
    public int hashCode() {
        return value.hashCode();
    }

    @Override
    public String toString() {
        return value;
    }
}
