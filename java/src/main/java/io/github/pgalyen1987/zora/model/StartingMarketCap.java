// Generated by codegen/emit_java.py from Zora's OpenAPI spec. Do not edit.
package io.github.pgalyen1987.zora.model;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Arrays;
import java.util.Collections;
import java.util.List;
import java.util.Objects;

/**
 * The starting market cap tier for a new coin. An extensible enum: values Zora adds later are kept,
 * not rejected.
 */
public final class StartingMarketCap {
    /** {@code LOW} */
    public static final StartingMarketCap LOW = new StartingMarketCap("LOW");
    /** {@code HIGH} */
    public static final StartingMarketCap HIGH = new StartingMarketCap("HIGH");

    /** Every value this SDK knows. */
    public static final List<StartingMarketCap> ALL = Collections.unmodifiableList(Arrays.asList(LOW, HIGH));

    private final String value;

    private StartingMarketCap(String value) {
        this.value = Objects.requireNonNull(value);
    }

    /** Wraps any value, including ones this SDK doesn't know. */
    @JsonCreator
    public static StartingMarketCap of(String value) {
        for (StartingMarketCap known : ALL) if (known.value.equals(value)) return known;
        return new StartingMarketCap(value);
    }

    /** The value as the API spells it. */
    @JsonValue
    public String value() {
        return value;
    }

    @Override
    public boolean equals(Object o) {
        return o instanceof StartingMarketCap && (((StartingMarketCap) o).value).equals(value);
    }

    @Override
    public int hashCode() {
        return value.hashCode();
    }

    @Override
    public String toString() {
        return value;
    }
}
