// Generated by codegen/emit_java.py from Zora's OpenAPI spec. Do not edit.
package io.github.pgalyen1987.zora.model;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import io.github.pgalyen1987.zora.internal.FlexBooleanDeserializer;
import java.util.ArrayList;
import java.util.List;

/** One page of SwapActivity results. Nodes() lists them; PageInfo says how to get the next page. */
@JsonIgnoreProperties(ignoreUnknown = true)
@JsonInclude(JsonInclude.Include.NON_NULL)
public final class SwapActivityConnection {
    @JsonProperty("count")
    private Long count;
    @JsonProperty("edges")
    private List<SwapActivityEdge> edges;
    @JsonProperty("pageInfo")
    private PageInfo pageInfo;

    /**
     * Total number of items, where the API reports it. May be null: absent unless the endpoint
     * selected it.
     */
    public Long getCount() {
        return count;
    }

    /** Sets {@link #getCount()}. Returns this, for chaining. */
    public SwapActivityConnection setCount(Long count) {
        this.count = count;
        return this;
    }

    /**
     * The items on this page, each wrapped in an edge. May be null: absent unless the endpoint
     * selected it.
     */
    public List<SwapActivityEdge> getEdges() {
        return edges;
    }

    /** Sets {@link #getEdges()}. Returns this, for chaining. */
    public SwapActivityConnection setEdges(List<SwapActivityEdge> edges) {
        this.edges = edges;
        return this;
    }

    /** How to fetch the next page. May be null: absent unless the endpoint selected it. */
    public PageInfo getPageInfo() {
        return pageInfo;
    }

    /** Sets {@link #getPageInfo()}. Returns this, for chaining. */
    public SwapActivityConnection setPageInfo(PageInfo pageInfo) {
        this.pageInfo = pageInfo;
        return this;
    }

    /** The SwapActivity values on this page, skipping empty edges. */
    public List<SwapActivity> nodes() {
        List<SwapActivity> out = new ArrayList<>();
        if (edges != null) for (SwapActivityEdge e : edges) if (e != null && e.getNode() != null) out.add(e.getNode());
        return out;
    }

    /** The cursor for the next page, or null on the last page. */
    public String nextCursor() {
        return pageInfo != null && Boolean.TRUE.equals(pageInfo.getHasNextPage()) ? pageInfo.getEndCursor() : null;
    }

    @Override
    public String toString() {
        return "SwapActivityConnection" + io.github.pgalyen1987.zora.internal.Json.toString(this);
    }
}
