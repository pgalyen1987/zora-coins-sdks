// Generated by codegen/emit_cpp.py. Method bodies of zora::Client.
inline std::optional<ApiKey> Client::api_key(const std::string& api_key) {
    detail::Query q;
    q.add("apiKey", api_key);
    auto resp = send<ApiKeyResponse>("GET", "/apiKey", q, std::string());
    return resp.api_key;
}

inline std::optional<Zora20Token> Client::coin(const std::string& address, const CoinParams& params) {
    detail::Query q;
    q.add("address", address);
    q.add("chain", std::to_string(params.chain_id.value_or(kBaseChainId)));
    auto resp = send<CoinResponse>("GET", "/coin", q, std::string());
    return resp.zora20_token;
}

inline ZoraCommentConnection Client::coin_comments(const std::string& address, const CoinCommentsParams& params) {
    detail::Query q;
    q.add("address", address);
    q.add("chain", std::to_string(params.chain_id.value_or(kBaseChainId)));
    if (params.after) q.add("after", detail::format(*params.after));
    if (params.page_size) q.add("count", detail::format(*params.page_size));
    auto resp = send<CoinCommentsResponse>("GET", "/coinComments", q, std::string());
    const auto* cur0 = &resp;
    if (!cur0->zora20_token) return {};
    const auto* cur1 = &*cur0->zora20_token;
    if (!cur1->zora_comments) return {};
    const auto* cur2 = &*cur1->zora_comments;
    return *cur2;
}

inline void Client::for_each_coin_comments(const std::string& address, const CoinCommentsParams& params, const std::function<bool(const ZoraComment&)>& visit) {
    CoinCommentsParams p = params;
    std::set<std::string> seen;
    for (;;) {
        auto page = coin_comments(address, p);
        for (const auto& n : page.nodes()) if (!visit(n)) return;
        auto next = page.next_cursor();
        if (next.empty() || !seen.insert(next).second) return;
        p.after = next;
    }
}

inline TokenBalanceConnection Client::coin_holders(const std::string& address, const CoinHoldersParams& params) {
    detail::Query q;
    q.add("chainId", std::to_string(params.chain_id.value_or(kBaseChainId)));
    q.add("address", address);
    if (params.after) q.add("after", detail::format(*params.after));
    if (params.page_size) q.add("count", detail::format(*params.page_size));
    auto resp = send<CoinHoldersResponse>("GET", "/coinHolders", q, std::string());
    const auto* cur0 = &resp;
    if (!cur0->zora20_token) return {};
    const auto* cur1 = &*cur0->zora20_token;
    if (!cur1->token_balances) return {};
    const auto* cur2 = &*cur1->token_balances;
    return *cur2;
}

inline void Client::for_each_coin_holders(const std::string& address, const CoinHoldersParams& params, const std::function<bool(const TokenBalance&)>& visit) {
    CoinHoldersParams p = params;
    std::set<std::string> seen;
    for (;;) {
        auto page = coin_holders(address, p);
        for (const auto& n : page.nodes()) if (!visit(n)) return;
        auto next = page.next_cursor();
        if (next.empty() || !seen.insert(next).second) return;
        p.after = next;
    }
}

inline CommentConnection Client::coin_merged_comments(const std::string& address, const CoinMergedCommentsParams& params) {
    detail::Query q;
    q.add("address", address);
    q.add("chain", std::to_string(params.chain_id.value_or(kBaseChainId)));
    if (params.after) q.add("after", detail::format(*params.after));
    if (params.page_size) q.add("count", detail::format(*params.page_size));
    auto resp = send<CoinMergedCommentsResponse>("GET", "/coinMergedComments", q, std::string());
    const auto* cur0 = &resp;
    if (!cur0->zora20_token) return {};
    const auto* cur1 = &*cur0->zora20_token;
    if (!cur1->comments) return {};
    const auto* cur2 = &*cur1->comments;
    return *cur2;
}

inline void Client::for_each_coin_merged_comments(const std::string& address, const CoinMergedCommentsParams& params, const std::function<bool(const Comment&)>& visit) {
    CoinMergedCommentsParams p = params;
    std::set<std::string> seen;
    for (;;) {
        auto page = coin_merged_comments(address, p);
        for (const auto& n : page.nodes()) if (!visit(n)) return;
        auto next = page.next_cursor();
        if (next.empty() || !seen.insert(next).second) return;
        p.after = next;
    }
}

inline std::optional<Zora20Token> Client::coin_price_history(const std::string& address, const CoinPriceHistoryParams& params) {
    detail::Query q;
    q.add("address", address);
    q.add("chain", std::to_string(params.chain_id.value_or(kBaseChainId)));
    auto resp = send<CoinPriceHistoryResponse>("GET", "/coinPriceHistory", q, std::string());
    return resp.zora20_token;
}

inline SwapActivityConnection Client::coin_swaps(const std::string& address, const CoinSwapsParams& params) {
    detail::Query q;
    q.add("address", address);
    q.add("chain", std::to_string(params.chain_id.value_or(kBaseChainId)));
    if (params.after) q.add("after", detail::format(*params.after));
    if (params.page_size) q.add("first", detail::format(*params.page_size));
    auto resp = send<CoinSwapsResponse>("GET", "/coinSwaps", q, std::string());
    const auto* cur0 = &resp;
    if (!cur0->zora20_token) return {};
    const auto* cur1 = &*cur0->zora20_token;
    if (!cur1->swap_activities) return {};
    const auto* cur2 = &*cur1->swap_activities;
    return *cur2;
}

inline void Client::for_each_coin_swaps(const std::string& address, const CoinSwapsParams& params, const std::function<bool(const SwapActivity&)>& visit) {
    CoinSwapsParams p = params;
    std::set<std::string> seen;
    for (;;) {
        auto page = coin_swaps(address, p);
        for (const auto& n : page.nodes()) if (!visit(n)) return;
        auto next = page.next_cursor();
        if (next.empty() || !seen.insert(next).second) return;
        p.after = next;
    }
}

inline std::vector<Zora20Token> Client::coins(const std::vector<CoinRefInput>& coins) {
    detail::Query q;
    for (const auto& item : coins) q.add("coins", nlohmann::json(item).dump());
    auto resp = send<CoinsResponse>("GET", "/coins", q, std::string());
    return resp.zora20_tokens.value_or(std::vector<Zora20Token>{});
}

inline CoinBasicInfoConnection Client::coins_list(const CoinsListParams& params) {
    detail::Query q;
    if (params.page_size) q.add("first", detail::format(*params.page_size));
    if (params.after) q.add("after", detail::format(*params.after));
    if (params.include_usdc_price) q.add("includeUSDCPrice", detail::format(*params.include_usdc_price));
    auto resp = send<CoinsListResponse>("GET", "/coinsList", q, std::string());
    const auto* cur0 = &resp;
    if (!cur0->coins_basic_info) return {};
    const auto* cur1 = &*cur0->coins_basic_info;
    return *cur1;
}

inline void Client::for_each_coins_list(const CoinsListParams& params, const std::function<bool(const CoinBasicInfo&)>& visit) {
    CoinsListParams p = params;
    std::set<std::string> seen;
    for (;;) {
        auto page = coins_list(p);
        for (const auto& n : page.nodes()) if (!visit(n)) return;
        auto next = page.next_cursor();
        if (next.empty() || !seen.insert(next).second) return;
        p.after = next;
    }
}

inline std::optional<PoolConfig> Client::content_coin_pool_config(const CurrencyType& currency_type, const ContentCoinPoolConfigParams& params) {
    detail::Query q;
    if (params.creator_identifier) q.add("creatorIdentifier", detail::format(*params.creator_identifier));
    q.add("currencyType", currency_type.value);
    auto resp = send<ContentCoinPoolConfigResponse>("GET", "/contentCoinPoolConfig", q, std::string());
    return resp.content_coin_pool_config;
}

inline std::optional<std::string> Client::create_upload_jwt(const CreateUploadJwtRequest& request) {
    detail::Query q;
    auto resp = send<CreateUploadJwtResponse>("POST", "/createUploadJWT", q, nlohmann::json(request).dump());
    return resp.create_upload_jwt_from_api_key;
}

inline std::optional<PoolConfig> Client::creator_coin_pool_config(const CreatorCoinPoolConfigParams& params) {
    detail::Query q;
    if (params.target_market_cap_usd) q.add("targetMarketCapUsd", detail::format(*params.target_market_cap_usd));
    auto resp = send<CreatorCoinPoolConfigResponse>("GET", "/creatorCoinPoolConfig", q, std::string());
    return resp.creator_coin_pool_config;
}

inline CommentConnection Client::creator_livestream_comments(const std::string& address, const CreatorLivestreamCommentsParams& params) {
    detail::Query q;
    q.add("address", address);
    q.add("chain", std::to_string(params.chain_id.value_or(kBaseChainId)));
    if (params.after) q.add("after", detail::format(*params.after));
    if (params.page_size) q.add("count", detail::format(*params.page_size));
    auto resp = send<CreatorLivestreamCommentsResponse>("GET", "/creatorLivestreamComments", q, std::string());
    const auto* cur0 = &resp;
    if (!cur0->zora20_token) return {};
    const auto* cur1 = &*cur0->zora20_token;
    if (!cur1->creator_profile) return {};
    const auto* cur2 = &*cur1->creator_profile;
    if (!cur2->live_stream) return {};
    const auto* cur3 = &*cur2->live_stream;
    if (!cur3->comments) return {};
    const auto* cur4 = &*cur3->comments;
    return *cur4;
}

inline void Client::for_each_creator_livestream_comments(const std::string& address, const CreatorLivestreamCommentsParams& params, const std::function<bool(const Comment&)>& visit) {
    CreatorLivestreamCommentsParams p = params;
    std::set<std::string> seen;
    for (;;) {
        auto page = creator_livestream_comments(address, p);
        for (const auto& n : page.nodes()) if (!visit(n)) return;
        auto next = page.next_cursor();
        if (next.empty() || !seen.insert(next).second) return;
        p.after = next;
    }
}

inline Zora20TokenConnection Client::explore(const ListType& list_type, const ExploreParams& params) {
    detail::Query q;
    q.add("listType", list_type.value);
    if (params.page_size) q.add("count", detail::format(*params.page_size));
    if (params.after) q.add("after", detail::format(*params.after));
    auto resp = send<ExploreResponse>("GET", "/explore", q, std::string());
    const auto* cur0 = &resp;
    if (!cur0->explore_list) return {};
    const auto* cur1 = &*cur0->explore_list;
    return *cur1;
}

inline void Client::for_each_explore(const ListType& list_type, const ExploreParams& params, const std::function<bool(const Zora20Token&)>& visit) {
    ExploreParams p = params;
    std::set<std::string> seen;
    for (;;) {
        auto page = explore(list_type, p);
        for (const auto& n : page.nodes()) if (!visit(n)) return;
        auto next = page.next_cursor();
        if (next.empty() || !seen.insert(next).second) return;
        p.after = next;
    }
}

inline FeaturedCreatorConnection Client::featured_creators(const FeaturedCreatorsParams& params) {
    detail::Query q;
    if (params.year) q.add("year", detail::format(*params.year));
    if (params.week) q.add("week", detail::format(*params.week));
    if (params.page_size) q.add("first", detail::format(*params.page_size));
    if (params.after) q.add("after", detail::format(*params.after));
    auto resp = send<FeaturedCreatorsResponse>("GET", "/featuredCreators", q, std::string());
    const auto* cur0 = &resp;
    if (!cur0->trader_leaderboard_featured_creators) return {};
    const auto* cur1 = &*cur0->trader_leaderboard_featured_creators;
    return *cur1;
}

inline void Client::for_each_featured_creators(const FeaturedCreatorsParams& params, const std::function<bool(const FeaturedCreator&)>& visit) {
    FeaturedCreatorsParams p = params;
    std::set<std::string> seen;
    for (;;) {
        auto page = featured_creators(p);
        for (const auto& n : page.nodes()) if (!visit(n)) return;
        auto next = page.next_cursor();
        if (next.empty() || !seen.insert(next).second) return;
        p.after = next;
    }
}

inline LiveStreamConnection Client::latest_live_streams(const LatestLiveStreamsParams& params) {
    detail::Query q;
    if (params.page_size) q.add("first", detail::format(*params.page_size));
    if (params.after) q.add("after", detail::format(*params.after));
    auto resp = send<LatestLiveStreamsResponse>("GET", "/latestLiveStreams", q, std::string());
    const auto* cur0 = &resp;
    if (!cur0->latest_live_streams) return {};
    const auto* cur1 = &*cur0->latest_live_streams;
    return *cur1;
}

inline void Client::for_each_latest_live_streams(const LatestLiveStreamsParams& params, const std::function<bool(const LiveStream&)>& visit) {
    LatestLiveStreamsParams p = params;
    std::set<std::string> seen;
    for (;;) {
        auto page = latest_live_streams(p);
        for (const auto& n : page.nodes()) if (!visit(n)) return;
        auto next = page.next_cursor();
        if (next.empty() || !seen.insert(next).second) return;
        p.after = next;
    }
}

inline std::optional<Profile> Client::profile(const std::string& identifier) {
    detail::Query q;
    q.add("identifier", identifier);
    auto resp = send<ProfileResponse>("GET", "/profile", q, std::string());
    return resp.profile;
}

inline CoinBalanceConnection Client::profile_balances(const std::string& identifier, const ProfileBalancesParams& params) {
    detail::Query q;
    q.add("identifier", identifier);
    if (params.page_size) q.add("count", detail::format(*params.page_size));
    if (params.after) q.add("after", detail::format(*params.after));
    if (params.sort_option) q.add("sortOption", detail::format(*params.sort_option));
    if (params.exclude_hidden) q.add("excludeHidden", detail::format(*params.exclude_hidden));
    if (params.chain_ids) for (const auto& v : *params.chain_ids) q.add("chainIds", detail::format(v));
    auto resp = send<ProfileBalancesResponse>("GET", "/profileBalances", q, std::string());
    const auto* cur0 = &resp;
    if (!cur0->profile) return {};
    const auto* cur1 = &*cur0->profile;
    if (!cur1->coin_balances) return {};
    const auto* cur2 = &*cur1->coin_balances;
    return *cur2;
}

inline void Client::for_each_profile_balances(const std::string& identifier, const ProfileBalancesParams& params, const std::function<bool(const CoinBalance&)>& visit) {
    ProfileBalancesParams p = params;
    std::set<std::string> seen;
    for (;;) {
        auto page = profile_balances(identifier, p);
        for (const auto& n : page.nodes()) if (!visit(n)) return;
        auto next = page.next_cursor();
        if (next.empty() || !seen.insert(next).second) return;
        p.after = next;
    }
}

inline std::optional<ProfileBySocialHandle> Client::profile_by_social_handle(const Platform& platform, const std::string& handle) {
    detail::Query q;
    q.add("platform", platform.value);
    q.add("handle", handle);
    auto resp = send<ProfileBySocialHandleResponse>("GET", "/profileBySocialHandle", q, std::string());
    return resp.profile_by_social_handle;
}

inline Zora20TokenConnection Client::profile_coins(const std::string& identifier, const ProfileCoinsParams& params) {
    detail::Query q;
    q.add("identifier", identifier);
    if (params.page_size) q.add("count", detail::format(*params.page_size));
    if (params.after) q.add("after", detail::format(*params.after));
    if (params.chain_ids) for (const auto& v : *params.chain_ids) q.add("chainIds", detail::format(v));
    if (params.platform_referrer_address) for (const auto& v : *params.platform_referrer_address) q.add("platformReferrerAddress", detail::format(v));
    auto resp = send<ProfileCoinsResponse>("GET", "/profileCoins", q, std::string());
    const auto* cur0 = &resp;
    if (!cur0->profile) return {};
    const auto* cur1 = &*cur0->profile;
    if (!cur1->created_coins) return {};
    const auto* cur2 = &*cur1->created_coins;
    return *cur2;
}

inline void Client::for_each_profile_coins(const std::string& identifier, const ProfileCoinsParams& params, const std::function<bool(const Zora20Token&)>& visit) {
    ProfileCoinsParams p = params;
    std::set<std::string> seen;
    for (;;) {
        auto page = profile_coins(identifier, p);
        for (const auto& n : page.nodes()) if (!visit(n)) return;
        auto next = page.next_cursor();
        if (next.empty() || !seen.insert(next).second) return;
        p.after = next;
    }
}

inline std::optional<Profile> Client::profile_social(const std::string& identifier) {
    detail::Query q;
    q.add("identifier", identifier);
    auto resp = send<ProfileSocialResponse>("GET", "/profileSocial", q, std::string());
    return resp.profile;
}

inline SearchResultConnection Client::search(const std::string& text, const SearchParams& params) {
    detail::Query q;
    q.add("text", text);
    if (params.entity_type) q.add("entityType", detail::format(*params.entity_type));
    if (params.page_size) q.add("first", detail::format(*params.page_size));
    if (params.after) q.add("after", detail::format(*params.after));
    auto resp = send<SearchResponse>("GET", "/search", q, std::string());
    const auto* cur0 = &resp;
    if (!cur0->global_search) return {};
    const auto* cur1 = &*cur0->global_search;
    return *cur1;
}

inline void Client::for_each_search(const std::string& text, const SearchParams& params, const std::function<bool(const SearchResult&)>& visit) {
    SearchParams p = params;
    std::set<std::string> seen;
    for (;;) {
        auto page = search(text, p);
        for (const auto& n : page.nodes()) if (!visit(n)) return;
        auto next = page.next_cursor();
        if (next.empty() || !seen.insert(next).second) return;
        p.after = next;
    }
}

inline std::optional<Erc20Token> Client::token_info(const std::string& address, const TokenInfoParams& params) {
    detail::Query q;
    q.add("address", address);
    q.add("chainId", std::to_string(params.chain_id.value_or(kBaseChainId)));
    auto resp = send<TokenInfoResponse>("GET", "/tokenInfo", q, std::string());
    return resp.erc20_token;
}

inline LiveStreamConnection Client::top_live_streams(const TopLiveStreamsParams& params) {
    detail::Query q;
    if (params.page_size) q.add("first", detail::format(*params.page_size));
    if (params.after) q.add("after", detail::format(*params.after));
    auto resp = send<TopLiveStreamsResponse>("GET", "/topLiveStreams", q, std::string());
    const auto* cur0 = &resp;
    if (!cur0->top_live_streams) return {};
    const auto* cur1 = &*cur0->top_live_streams;
    return *cur1;
}

inline void Client::for_each_top_live_streams(const TopLiveStreamsParams& params, const std::function<bool(const LiveStream&)>& visit) {
    TopLiveStreamsParams p = params;
    std::set<std::string> seen;
    for (;;) {
        auto page = top_live_streams(p);
        for (const auto& n : page.nodes()) if (!visit(n)) return;
        auto next = page.next_cursor();
        if (next.empty() || !seen.insert(next).second) return;
        p.after = next;
    }
}

inline TraderLeaderboardEntryConnection Client::trader_leaderboard(const TraderLeaderboardParams& params) {
    detail::Query q;
    if (params.week) q.add("week", detail::format(*params.week));
    if (params.year) q.add("year", detail::format(*params.year));
    if (params.page_size) q.add("first", detail::format(*params.page_size));
    if (params.after) q.add("after", detail::format(*params.after));
    auto resp = send<TraderLeaderboardResponse>("GET", "/traderLeaderboard", q, std::string());
    const auto* cur0 = &resp;
    if (!cur0->explore_trader_leaderboard) return {};
    const auto* cur1 = &*cur0->explore_trader_leaderboard;
    return *cur1;
}

inline void Client::for_each_trader_leaderboard(const TraderLeaderboardParams& params, const std::function<bool(const TraderLeaderboardEntry&)>& visit) {
    TraderLeaderboardParams p = params;
    std::set<std::string> seen;
    for (;;) {
        auto page = trader_leaderboard(p);
        for (const auto& n : page.nodes()) if (!visit(n)) return;
        auto next = page.next_cursor();
        if (next.empty() || !seen.insert(next).second) return;
        p.after = next;
    }
}

inline std::optional<Zora20Token> Client::trend_coin(const std::string& ticker, const TrendCoinParams& params) {
    detail::Query q;
    q.add("ticker", ticker);
    q.add("chainId", std::to_string(params.chain_id.value_or(kBaseChainId)));
    auto resp = send<TrendCoinResponse>("GET", "/trendCoin", q, std::string());
    return resp.trend_coin;
}

inline Zora20TokenConnection Client::trends_by_name(const std::string& name, const TrendsByNameParams& params) {
    detail::Query q;
    q.add("name", name);
    if (params.page_size) q.add("first", detail::format(*params.page_size));
    if (params.after) q.add("after", detail::format(*params.after));
    auto resp = send<TrendsByNameResponse>("GET", "/trendsByName", q, std::string());
    const auto* cur0 = &resp;
    if (!cur0->trends_by_name) return {};
    const auto* cur1 = &*cur0->trends_by_name;
    return *cur1;
}

inline void Client::for_each_trends_by_name(const std::string& name, const TrendsByNameParams& params, const std::function<bool(const Zora20Token&)>& visit) {
    TrendsByNameParams p = params;
    std::set<std::string> seen;
    for (;;) {
        auto page = trends_by_name(name, p);
        for (const auto& n : page.nodes()) if (!visit(n)) return;
        auto next = page.next_cursor();
        if (next.empty() || !seen.insert(next).second) return;
        p.after = next;
    }
}

inline TradeActivityConnection Client::wallet_trade_activity(const std::string& identifier, const WalletTradeActivityParams& params) {
    detail::Query q;
    q.add("identifier", identifier);
    if (params.after) q.add("after", detail::format(*params.after));
    if (params.page_size) q.add("first", detail::format(*params.page_size));
    auto resp = send<WalletTradeActivityResponse>("GET", "/walletTradeActivity", q, std::string());
    const auto* cur0 = &resp;
    if (!cur0->wallet_address_trade_activity) return {};
    const auto* cur1 = &*cur0->wallet_address_trade_activity;
    return *cur1;
}

inline void Client::for_each_wallet_trade_activity(const std::string& identifier, const WalletTradeActivityParams& params, const std::function<bool(const TradeActivity&)>& visit) {
    WalletTradeActivityParams p = params;
    std::set<std::string> seen;
    for (;;) {
        auto page = wallet_trade_activity(identifier, p);
        for (const auto& n : page.nodes()) if (!visit(n)) return;
        auto next = page.next_cursor();
        if (next.empty() || !seen.insert(next).second) return;
        p.after = next;
    }
}

inline QuoteResponse Client::quote(const QuoteRequest& request) {
    detail::Query q;
    auto resp = send<QuoteResponse>("POST", "/quote", q, nlohmann::json(request).dump());
    return resp;
}

inline CreateContentCoinResponse Client::create_content_coin(const CreateContentCoinRequest& request) {
    detail::Query q;
    auto resp = send<CreateContentCoinResponse>("POST", "/create/content", q, nlohmann::json(request).dump());
    return resp;
}

