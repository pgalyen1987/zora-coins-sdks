// Code generated by codegen/emit_ts.py from Zora's OpenAPI spec. DO NOT EDIT.

import { BASE_CHAIN_ID, BaseClient, paginate } from "./base.js";
import type * as M from "./models.js";

/** Optional parameters of `ZoraCoins.coin`. */
export interface CoinParams {
  /** Chain ID. Defaults to Base (8453). */
  chainId?: number;
}

/** Optional parameters of `ZoraCoins.coinComments`. */
export interface CoinCommentsParams {
  /** Chain ID. Defaults to Base (8453). */
  chainId?: number;
  /** Cursor to continue from: the previous page's pageInfo.endCursor. */
  after?: string;
  /** Page size. */
  pageSize?: number;
}

/** Optional parameters of `ZoraCoins.coinHolders`. */
export interface CoinHoldersParams {
  /** Chain ID. Defaults to Base (8453). */
  chainId?: number;
  /** Cursor to continue from: the previous page's pageInfo.endCursor. */
  after?: string;
  /** Page size. */
  pageSize?: number;
}

/** Optional parameters of `ZoraCoins.coinMergedComments`. */
export interface CoinMergedCommentsParams {
  /** Chain ID. Defaults to Base (8453). */
  chainId?: number;
  /** Cursor to continue from: the previous page's pageInfo.endCursor. */
  after?: string;
  /** Page size. */
  pageSize?: number;
}

/** Optional parameters of `ZoraCoins.coinPriceHistory`. */
export interface CoinPriceHistoryParams {
  /** Chain ID. Defaults to Base (8453). */
  chainId?: number;
}

/** Optional parameters of `ZoraCoins.coinSwaps`. */
export interface CoinSwapsParams {
  /** Chain ID. Defaults to Base (8453). */
  chainId?: number;
  /** Cursor to continue from: the previous page's pageInfo.endCursor. */
  after?: string;
  /** Page size. */
  pageSize?: number;
}

/** Optional parameters of `ZoraCoins.coinsList`. */
export interface CoinsListParams {
  /** Page size. */
  pageSize?: number;
  /** Cursor to continue from: the previous page's pageInfo.endCursor. */
  after?: string;
  /** Include each coin's price in USDC. Defaults to true. */
  includeUSDCPrice?: boolean;
}

/** Optional parameters of `ZoraCoins.contentCoinPoolConfig`. */
export interface ContentCoinPoolConfigParams {
  /** Profile handle or wallet of the creator, used to pair with their creator coin. */
  creatorIdentifier?: string;
}

/** Optional parameters of `ZoraCoins.creatorCoinPoolConfig`. */
export interface CreatorCoinPoolConfigParams {
  /** Starting market cap to size the pool for, in USD. */
  targetMarketCapUsd?: number;
}

/** Optional parameters of `ZoraCoins.creatorLivestreamComments`. */
export interface CreatorLivestreamCommentsParams {
  /** Chain ID. Defaults to Base (8453). */
  chainId?: number;
  /** Cursor to continue from: the previous page's pageInfo.endCursor. */
  after?: string;
  /** Page size. */
  pageSize?: number;
}

/** Optional parameters of `ZoraCoins.explore`. */
export interface ExploreParams {
  /** Page size. */
  pageSize?: number;
  /** Cursor to continue from: the previous page's pageInfo.endCursor. */
  after?: string;
}

/** Optional parameters of `ZoraCoins.featuredCreators`. */
export interface FeaturedCreatorsParams {
  /** Year of the ISO week. Defaults to the current year. */
  year?: number;
  /** ISO week number. Defaults to the current week. */
  week?: number;
  /** Page size. */
  pageSize?: number;
  /** Cursor to continue from: the previous page's pageInfo.endCursor. */
  after?: string;
}

/** Optional parameters of `ZoraCoins.latestLiveStreams`. */
export interface LatestLiveStreamsParams {
  /** Page size. */
  pageSize?: number;
  /** Cursor to continue from: the previous page's pageInfo.endCursor. */
  after?: string;
}

/** Optional parameters of `ZoraCoins.profileBalances`. */
export interface ProfileBalancesParams {
  /** Page size. */
  pageSize?: number;
  /** Cursor to continue from: the previous page's pageInfo.endCursor. */
  after?: string;
  /** How to order the balances. */
  sortOption?: M.SortOption;
  /** Leave out coins the profile has hidden. Defaults to true. */
  excludeHidden?: boolean;
  /** Only include these chains. Defaults to Base. */
  chainIds?: number[];
}

/** Optional parameters of `ZoraCoins.profileCoins`. */
export interface ProfileCoinsParams {
  /** Page size. */
  pageSize?: number;
  /** Cursor to continue from: the previous page's pageInfo.endCursor. */
  after?: string;
  /** Only include these chains. Defaults to Base. */
  chainIds?: number[];
  /** Only coins created through one of these platform referrer addresses. */
  platformReferrerAddress?: string[];
}

/** Optional parameters of `ZoraCoins.search`. */
export interface SearchParams {
  /** Only return results of this kind. */
  entityType?: M.EntityType;
  /** Page size. */
  pageSize?: number;
  /** Cursor to continue from: the previous page's pageInfo.endCursor. */
  after?: string;
}

/** Optional parameters of `ZoraCoins.tokenInfo`. */
export interface TokenInfoParams {
  /** Chain ID. Defaults to Base (8453). */
  chainId?: number;
}

/** Optional parameters of `ZoraCoins.topLiveStreams`. */
export interface TopLiveStreamsParams {
  /** Page size. */
  pageSize?: number;
  /** Cursor to continue from: the previous page's pageInfo.endCursor. */
  after?: string;
}

/** Optional parameters of `ZoraCoins.traderLeaderboard`. */
export interface TraderLeaderboardParams {
  /** ISO week number. Defaults to the current week. */
  week?: number;
  /** Year of the ISO week. Defaults to the current year. */
  year?: number;
  /** Page size. */
  pageSize?: number;
  /** Cursor to continue from: the previous page's pageInfo.endCursor. */
  after?: string;
}

/** Optional parameters of `ZoraCoins.trendCoin`. */
export interface TrendCoinParams {
  /** Chain ID. Defaults to Base (8453). */
  chainId?: number;
}

/** Optional parameters of `ZoraCoins.trendsByName`. */
export interface TrendsByNameParams {
  /** Page size. */
  pageSize?: number;
  /** Cursor to continue from: the previous page's pageInfo.endCursor. */
  after?: string;
}

/** Optional parameters of `ZoraCoins.walletTradeActivity`. */
export interface WalletTradeActivityParams {
  /** Cursor to continue from: the previous page's pageInfo.endCursor. */
  after?: string;
  /** Page size. */
  pageSize?: number;
}

/**
 * Client for the Zora Coins API: one method per endpoint, and an `iter…` async generator for every
 * paginated one. Use {@link ZoraCoins}, which adds conveniences on top.
 */
export class GeneratedClient extends BaseClient {
  /** Look up an API key: whether it exists and is active. `GET /apiKey` */
  async apiKey(apiKey: string): Promise<M.ApiKey | null> {
    const q: Array<[string, string]> = [];
    q.push(["apiKey", String(apiKey)]);
    const resp = await this.request<M.ApiKeyResponse>("GET", "/apiKey", q);
    return resp.apiKey ?? null;
  }

  /**
   * One coin by contract address: market data, creator profile, media, pool key and creator
   * earnings. Resolves to `null` if there is no such coin. `GET /coin`
   */
  async coin(address: string, params: CoinParams = {}): Promise<M.Zora20Token | null> {
    const q: Array<[string, string]> = [];
    q.push(["address", String(address)]);
    q.push(["chain", String(params.chainId ?? BASE_CHAIN_ID)]);
    const resp = await this.request<M.CoinResponse>("GET", "/coin", q);
    return resp.zora20Token ?? null;
  }

  /**
   * Comments posted on a coin, newest first. Returns one page; `iterCoinComments` walks every
   * page. `GET /coinComments`
   */
  async coinComments(address: string, params: CoinCommentsParams = {}): Promise<M.ZoraCommentConnection> {
    const q: Array<[string, string]> = [];
    q.push(["address", String(address)]);
    q.push(["chain", String(params.chainId ?? BASE_CHAIN_ID)]);
    if (params.after !== undefined) q.push(["after", String(params.after)]);
    if (params.pageSize !== undefined) q.push(["count", String(params.pageSize)]);
    const resp = await this.request<M.CoinCommentsResponse>("GET", "/coinComments", q);
    return resp?.zora20Token?.zoraComments ?? {};
  }

  /**
   * Every ZoraComment across all pages, fetched as the loop reaches them. `break` stops early;
   * `params.after` picks the starting point.
   */
  iterCoinComments(address: string, params: CoinCommentsParams = {}): AsyncGenerator<M.ZoraComment, void, undefined> {
    return paginate<CoinCommentsParams, M.ZoraComment>(params, async (p) => {
      const page = await this.coinComments(address, p);
      const nodes = (page.edges ?? []).flatMap((e) => (e?.node ? [e.node] : []));
      return [nodes, page.pageInfo?.hasNextPage ? page.pageInfo.endCursor ?? undefined : undefined];
    });
  }

  /**
   * Holders of a coin and their balances, largest first. Returns one page; `iterCoinHolders` walks
   * every page. `GET /coinHolders`
   */
  async coinHolders(address: string, params: CoinHoldersParams = {}): Promise<M.TokenBalanceConnection> {
    const q: Array<[string, string]> = [];
    q.push(["chainId", String(params.chainId ?? BASE_CHAIN_ID)]);
    q.push(["address", String(address)]);
    if (params.after !== undefined) q.push(["after", String(params.after)]);
    if (params.pageSize !== undefined) q.push(["count", String(params.pageSize)]);
    const resp = await this.request<M.CoinHoldersResponse>("GET", "/coinHolders", q);
    return resp?.zora20Token?.tokenBalances ?? {};
  }

  /**
   * Every TokenBalance across all pages, fetched as the loop reaches them. `break` stops early;
   * `params.after` picks the starting point.
   */
  iterCoinHolders(address: string, params: CoinHoldersParams = {}): AsyncGenerator<M.TokenBalance, void, undefined> {
    return paginate<CoinHoldersParams, M.TokenBalance>(params, async (p) => {
      const page = await this.coinHolders(address, p);
      const nodes = (page.edges ?? []).flatMap((e) => (e?.node ? [e.node] : []));
      return [nodes, page.pageInfo?.hasNextPage ? page.pageInfo.endCursor ?? undefined : undefined];
    });
  }

  /**
   * A coin's comment feed with replies threaded in, newest first. Returns one page;
   * `iterCoinMergedComments` walks every page. `GET /coinMergedComments`
   */
  async coinMergedComments(address: string, params: CoinMergedCommentsParams = {}): Promise<M.CommentConnection> {
    const q: Array<[string, string]> = [];
    q.push(["address", String(address)]);
    q.push(["chain", String(params.chainId ?? BASE_CHAIN_ID)]);
    if (params.after !== undefined) q.push(["after", String(params.after)]);
    if (params.pageSize !== undefined) q.push(["count", String(params.pageSize)]);
    const resp = await this.request<M.CoinMergedCommentsResponse>("GET", "/coinMergedComments", q);
    return resp?.zora20Token?.comments ?? {};
  }

  /**
   * Every Comment across all pages, fetched as the loop reaches them. `break` stops early;
   * `params.after` picks the starting point.
   */
  iterCoinMergedComments(address: string, params: CoinMergedCommentsParams = {}): AsyncGenerator<M.Comment, void, undefined> {
    return paginate<CoinMergedCommentsParams, M.Comment>(params, async (p) => {
      const page = await this.coinMergedComments(address, p);
      const nodes = (page.edges ?? []).flatMap((e) => (e?.node ? [e.node] : []));
      return [nodes, page.pageInfo?.hasNextPage ? page.pageInfo.endCursor ?? undefined : undefined];
    });
  }

  /**
   * A coin's price history at five resolutions: the last hour, day, week, month, and all time.
   * Resolves to `null` if there is no such coin. `GET /coinPriceHistory`
   */
  async coinPriceHistory(address: string, params: CoinPriceHistoryParams = {}): Promise<M.Zora20Token | null> {
    const q: Array<[string, string]> = [];
    q.push(["address", String(address)]);
    q.push(["chain", String(params.chainId ?? BASE_CHAIN_ID)]);
    const resp = await this.request<M.CoinPriceHistoryResponse>("GET", "/coinPriceHistory", q);
    return resp.zora20Token ?? null;
  }

  /**
   * Buys and sells of a coin, newest first. Returns one page; `iterCoinSwaps` walks every page.
   * `GET /coinSwaps`
   */
  async coinSwaps(address: string, params: CoinSwapsParams = {}): Promise<M.SwapActivityConnection> {
    const q: Array<[string, string]> = [];
    q.push(["address", String(address)]);
    q.push(["chain", String(params.chainId ?? BASE_CHAIN_ID)]);
    if (params.after !== undefined) q.push(["after", String(params.after)]);
    if (params.pageSize !== undefined) q.push(["first", String(params.pageSize)]);
    const resp = await this.request<M.CoinSwapsResponse>("GET", "/coinSwaps", q);
    return resp?.zora20Token?.swapActivities ?? {};
  }

  /**
   * Every SwapActivity across all pages, fetched as the loop reaches them. `break` stops early;
   * `params.after` picks the starting point.
   */
  iterCoinSwaps(address: string, params: CoinSwapsParams = {}): AsyncGenerator<M.SwapActivity, void, undefined> {
    return paginate<CoinSwapsParams, M.SwapActivity>(params, async (p) => {
      const page = await this.coinSwaps(address, p);
      const nodes = (page.edges ?? []).flatMap((e) => (e?.node ? [e.node] : []));
      return [nodes, page.pageInfo?.hasNextPage ? page.pageInfo.endCursor ?? undefined : undefined];
    });
  }

  /** Several coins in one request. Missing coins are left out of the result. `GET /coins` */
  async coins(coins: M.CoinRefInput[]): Promise<M.Zora20Token[]> {
    const q: Array<[string, string]> = [];
    for (const item of coins) q.push(["coins", JSON.stringify(item)]);
    const resp = await this.request<M.CoinsResponse>("GET", "/coins", q);
    return resp.zora20Tokens ?? [];
  }

  /**
   * Basic information (address, name, symbol, price) for every coin, in pages of up to 1,000.
   * Returns one page; `iterCoinsList` walks every page. `GET /coinsList`
   */
  async coinsList(params: CoinsListParams = {}): Promise<M.CoinBasicInfoConnection> {
    const q: Array<[string, string]> = [];
    if (params.pageSize !== undefined) q.push(["first", String(params.pageSize)]);
    if (params.after !== undefined) q.push(["after", String(params.after)]);
    if (params.includeUSDCPrice !== undefined) q.push(["includeUSDCPrice", String(params.includeUSDCPrice)]);
    const resp = await this.request<M.CoinsListResponse>("GET", "/coinsList", q);
    return resp?.coinsBasicInfo ?? {};
  }

  /**
   * Every CoinBasicInfo across all pages, fetched as the loop reaches them. `break` stops early;
   * `params.after` picks the starting point.
   */
  iterCoinsList(params: CoinsListParams = {}): AsyncGenerator<M.CoinBasicInfo, void, undefined> {
    return paginate<CoinsListParams, M.CoinBasicInfo>(params, async (p) => {
      const page = await this.coinsList(p);
      const nodes = (page.edges ?? []).flatMap((e) => (e?.node ? [e.node] : []));
      return [nodes, page.pageInfo?.hasNextPage ? page.pageInfo.endCursor ?? undefined : undefined];
    });
  }

  /**
   * The Uniswap V4 pool configuration Zora uses to launch a content coin paired with the given
   * currency. `GET /contentCoinPoolConfig`
   */
  async contentCoinPoolConfig(currencyType: M.CurrencyType, params: ContentCoinPoolConfigParams = {}): Promise<M.PoolConfig | null> {
    const q: Array<[string, string]> = [];
    if (params.creatorIdentifier !== undefined) q.push(["creatorIdentifier", String(params.creatorIdentifier)]);
    q.push(["currencyType", String(currencyType)]);
    const resp = await this.request<M.ContentCoinPoolConfigResponse>("GET", "/contentCoinPoolConfig", q);
    return resp.contentCoinPoolConfig ?? null;
  }

  /**
   * Create a short-lived token for uploading coin media and metadata to Zora's uploader. Requires
   * an API key. `POST /createUploadJWT`
   */
  async createUploadJwt(req: M.CreateUploadJwtRequest): Promise<string | null> {
    const q: Array<[string, string]> = [];
    const resp = await this.request<M.CreateUploadJwtResponse>("POST", "/createUploadJWT", q, req);
    return resp.createUploadJwtFromApiKey ?? null;
  }

  /**
   * The Uniswap V4 pool configuration Zora uses to launch a creator coin, optionally sized for a
   * starting market cap in USD. `GET /creatorCoinPoolConfig`
   */
  async creatorCoinPoolConfig(params: CreatorCoinPoolConfigParams = {}): Promise<M.PoolConfig | null> {
    const q: Array<[string, string]> = [];
    if (params.targetMarketCapUsd !== undefined) q.push(["targetMarketCapUsd", String(params.targetMarketCapUsd)]);
    const resp = await this.request<M.CreatorCoinPoolConfigResponse>("GET", "/creatorCoinPoolConfig", q);
    return resp.creatorCoinPoolConfig ?? null;
  }

  /**
   * Comments on the live stream of the creator behind a coin. Returns one page;
   * `iterCreatorLivestreamComments` walks every page. `GET /creatorLivestreamComments`
   */
  async creatorLivestreamComments(address: string, params: CreatorLivestreamCommentsParams = {}): Promise<M.CommentConnection> {
    const q: Array<[string, string]> = [];
    q.push(["address", String(address)]);
    q.push(["chain", String(params.chainId ?? BASE_CHAIN_ID)]);
    if (params.after !== undefined) q.push(["after", String(params.after)]);
    if (params.pageSize !== undefined) q.push(["count", String(params.pageSize)]);
    const resp = await this.request<M.CreatorLivestreamCommentsResponse>("GET", "/creatorLivestreamComments", q);
    return resp?.zora20Token?.creatorProfile?.liveStream?.comments ?? {};
  }

  /**
   * Every Comment across all pages, fetched as the loop reaches them. `break` stops early;
   * `params.after` picks the starting point.
   */
  iterCreatorLivestreamComments(address: string, params: CreatorLivestreamCommentsParams = {}): AsyncGenerator<M.Comment, void, undefined> {
    return paginate<CreatorLivestreamCommentsParams, M.Comment>(params, async (p) => {
      const page = await this.creatorLivestreamComments(address, p);
      const nodes = (page.edges ?? []).flatMap((e) => (e?.node ? [e.node] : []));
      return [nodes, page.pageInfo?.hasNextPage ? page.pageInfo.endCursor ?? undefined : undefined];
    });
  }

  /**
   * A Zora Explore list: top gainers, 24h volume, newest, most valuable creators and more. See
   * ListType for all 25. Returns one page; `iterExplore` walks every page. `GET /explore`
   */
  async explore(listType: M.ListType, params: ExploreParams = {}): Promise<M.Zora20TokenConnection> {
    const q: Array<[string, string]> = [];
    q.push(["listType", String(listType)]);
    if (params.pageSize !== undefined) q.push(["count", String(params.pageSize)]);
    if (params.after !== undefined) q.push(["after", String(params.after)]);
    const resp = await this.request<M.ExploreResponse>("GET", "/explore", q);
    return resp?.exploreList ?? {};
  }

  /**
   * Every Zora20Token across all pages, fetched as the loop reaches them. `break` stops early;
   * `params.after` picks the starting point.
   */
  iterExplore(listType: M.ListType, params: ExploreParams = {}): AsyncGenerator<M.Zora20Token, void, undefined> {
    return paginate<ExploreParams, M.Zora20Token>(params, async (p) => {
      const page = await this.explore(listType, p);
      const nodes = (page.edges ?? []).flatMap((e) => (e?.node ? [e.node] : []));
      return [nodes, page.pageInfo?.hasNextPage ? page.pageInfo.endCursor ?? undefined : undefined];
    });
  }

  /**
   * Creators featured on the weekly leaderboard. Defaults to the current week. Returns one page;
   * `iterFeaturedCreators` walks every page. `GET /featuredCreators`
   */
  async featuredCreators(params: FeaturedCreatorsParams = {}): Promise<M.FeaturedCreatorConnection> {
    const q: Array<[string, string]> = [];
    if (params.year !== undefined) q.push(["year", String(params.year)]);
    if (params.week !== undefined) q.push(["week", String(params.week)]);
    if (params.pageSize !== undefined) q.push(["first", String(params.pageSize)]);
    if (params.after !== undefined) q.push(["after", String(params.after)]);
    const resp = await this.request<M.FeaturedCreatorsResponse>("GET", "/featuredCreators", q);
    return resp?.traderLeaderboardFeaturedCreators ?? {};
  }

  /**
   * Every FeaturedCreator across all pages, fetched as the loop reaches them. `break` stops early;
   * `params.after` picks the starting point.
   */
  iterFeaturedCreators(params: FeaturedCreatorsParams = {}): AsyncGenerator<M.FeaturedCreator, void, undefined> {
    return paginate<FeaturedCreatorsParams, M.FeaturedCreator>(params, async (p) => {
      const page = await this.featuredCreators(p);
      const nodes = (page.edges ?? []).flatMap((e) => (e?.node ? [e.node] : []));
      return [nodes, page.pageInfo?.hasNextPage ? page.pageInfo.endCursor ?? undefined : undefined];
    });
  }

  /**
   * Live streams, most recently started first. Returns one page; `iterLatestLiveStreams` walks
   * every page. `GET /latestLiveStreams`
   */
  async latestLiveStreams(params: LatestLiveStreamsParams = {}): Promise<M.LiveStreamConnection> {
    const q: Array<[string, string]> = [];
    if (params.pageSize !== undefined) q.push(["first", String(params.pageSize)]);
    if (params.after !== undefined) q.push(["after", String(params.after)]);
    const resp = await this.request<M.LatestLiveStreamsResponse>("GET", "/latestLiveStreams", q);
    return resp?.latestLiveStreams ?? {};
  }

  /**
   * Every LiveStream across all pages, fetched as the loop reaches them. `break` stops early;
   * `params.after` picks the starting point.
   */
  iterLatestLiveStreams(params: LatestLiveStreamsParams = {}): AsyncGenerator<M.LiveStream, void, undefined> {
    return paginate<LatestLiveStreamsParams, M.LiveStream>(params, async (p) => {
      const page = await this.latestLiveStreams(p);
      const nodes = (page.edges ?? []).flatMap((e) => (e?.node ? [e.node] : []));
      return [nodes, page.pageInfo?.hasNextPage ? page.pageInfo.endCursor ?? undefined : undefined];
    });
  }

  /**
   * A profile by handle or wallet address, with its linked wallets and creator coin. Resolves to
   * `null` if there is no such profile. `GET /profile`
   */
  async profile(identifier: string): Promise<M.Profile | null> {
    const q: Array<[string, string]> = [];
    q.push(["identifier", String(identifier)]);
    const resp = await this.request<M.ProfileResponse>("GET", "/profile", q);
    return resp.profile ?? null;
  }

  /**
   * Coins a profile holds, with balances and current values. Returns one page;
   * `iterProfileBalances` walks every page. `GET /profileBalances`
   */
  async profileBalances(identifier: string, params: ProfileBalancesParams = {}): Promise<M.CoinBalanceConnection> {
    const q: Array<[string, string]> = [];
    q.push(["identifier", String(identifier)]);
    if (params.pageSize !== undefined) q.push(["count", String(params.pageSize)]);
    if (params.after !== undefined) q.push(["after", String(params.after)]);
    if (params.sortOption !== undefined) q.push(["sortOption", String(params.sortOption)]);
    if (params.excludeHidden !== undefined) q.push(["excludeHidden", String(params.excludeHidden)]);
    for (const v of params.chainIds ?? []) q.push(["chainIds", String(v)]);
    const resp = await this.request<M.ProfileBalancesResponse>("GET", "/profileBalances", q);
    return resp?.profile?.coinBalances ?? {};
  }

  /**
   * Every CoinBalance across all pages, fetched as the loop reaches them. `break` stops early;
   * `params.after` picks the starting point.
   */
  iterProfileBalances(identifier: string, params: ProfileBalancesParams = {}): AsyncGenerator<M.CoinBalance, void, undefined> {
    return paginate<ProfileBalancesParams, M.CoinBalance>(params, async (p) => {
      const page = await this.profileBalances(identifier, p);
      const nodes = (page.edges ?? []).flatMap((e) => (e?.node ? [e.node] : []));
      return [nodes, page.pageInfo?.hasNextPage ? page.pageInfo.endCursor ?? undefined : undefined];
    });
  }

  /**
   * Find a Zora profile from its handle on X, TikTok, Farcaster or Instagram. Resolves to `null`
   * if there is no such profile. `GET /profileBySocialHandle`
   */
  async profileBySocialHandle(platform: M.Platform, handle: string): Promise<M.ProfileBySocialHandle | null> {
    const q: Array<[string, string]> = [];
    q.push(["platform", String(platform)]);
    q.push(["handle", String(handle)]);
    const resp = await this.request<M.ProfileBySocialHandleResponse>("GET", "/profileBySocialHandle", q);
    return resp.profileBySocialHandle ?? null;
  }

  /**
   * Coins a profile created. Filter by platform referrer to list only coins launched through a
   * given app. Returns one page; `iterProfileCoins` walks every page. `GET /profileCoins`
   */
  async profileCoins(identifier: string, params: ProfileCoinsParams = {}): Promise<M.Zora20TokenConnection> {
    const q: Array<[string, string]> = [];
    q.push(["identifier", String(identifier)]);
    if (params.pageSize !== undefined) q.push(["count", String(params.pageSize)]);
    if (params.after !== undefined) q.push(["after", String(params.after)]);
    for (const v of params.chainIds ?? []) q.push(["chainIds", String(v)]);
    for (const v of params.platformReferrerAddress ?? []) q.push(["platformReferrerAddress", String(v)]);
    const resp = await this.request<M.ProfileCoinsResponse>("GET", "/profileCoins", q);
    return resp?.profile?.createdCoins ?? {};
  }

  /**
   * Every Zora20Token across all pages, fetched as the loop reaches them. `break` stops early;
   * `params.after` picks the starting point.
   */
  iterProfileCoins(identifier: string, params: ProfileCoinsParams = {}): AsyncGenerator<M.Zora20Token, void, undefined> {
    return paginate<ProfileCoinsParams, M.Zora20Token>(params, async (p) => {
      const page = await this.profileCoins(identifier, p);
      const nodes = (page.edges ?? []).flatMap((e) => (e?.node ? [e.node] : []));
      return [nodes, page.pageInfo?.hasNextPage ? page.pageInfo.endCursor ?? undefined : undefined];
    });
  }

  /**
   * A profile's linked social accounts (X, TikTok, Instagram, Farcaster) and follower counts.
   * Resolves to `null` if there is no such profile. `GET /profileSocial`
   */
  async profileSocial(identifier: string): Promise<M.Profile | null> {
    const q: Array<[string, string]> = [];
    q.push(["identifier", String(identifier)]);
    const resp = await this.request<M.ProfileSocialResponse>("GET", "/profileSocial", q);
    return resp.profile ?? null;
  }

  /**
   * Search coins, profiles, content and trends by text. Returns one page; `iterSearch` walks every
   * page. `GET /search`
   */
  async search(text: string, params: SearchParams = {}): Promise<M.SearchResultConnection> {
    const q: Array<[string, string]> = [];
    q.push(["text", String(text)]);
    if (params.entityType !== undefined) q.push(["entityType", String(params.entityType)]);
    if (params.pageSize !== undefined) q.push(["first", String(params.pageSize)]);
    if (params.after !== undefined) q.push(["after", String(params.after)]);
    const resp = await this.request<M.SearchResponse>("GET", "/search", q);
    return resp?.globalSearch ?? {};
  }

  /**
   * Every SearchResult across all pages, fetched as the loop reaches them. `break` stops early;
   * `params.after` picks the starting point.
   */
  iterSearch(text: string, params: SearchParams = {}): AsyncGenerator<M.SearchResult, void, undefined> {
    return paginate<SearchParams, M.SearchResult>(params, async (p) => {
      const page = await this.search(text, p);
      const nodes = (page.edges ?? []).flatMap((e) => (e?.node ? [e.node] : []));
      return [nodes, page.pageInfo?.hasNextPage ? page.pageInfo.endCursor ?? undefined : undefined];
    });
  }

  /**
   * Symbol, decimals and current USD price of any ERC-20 on the chain: ZORA, USDC, WETH, or a
   * coin. Resolves to `null` if there is no such coin. `GET /tokenInfo`
   */
  async tokenInfo(address: string, params: TokenInfoParams = {}): Promise<M.Erc20Token | null> {
    const q: Array<[string, string]> = [];
    q.push(["address", String(address)]);
    q.push(["chainId", String(params.chainId ?? BASE_CHAIN_ID)]);
    const resp = await this.request<M.TokenInfoResponse>("GET", "/tokenInfo", q);
    return resp.erc20Token ?? null;
  }

  /**
   * Live streams with the most viewers right now. Returns one page; `iterTopLiveStreams` walks
   * every page. `GET /topLiveStreams`
   */
  async topLiveStreams(params: TopLiveStreamsParams = {}): Promise<M.LiveStreamConnection> {
    const q: Array<[string, string]> = [];
    if (params.pageSize !== undefined) q.push(["first", String(params.pageSize)]);
    if (params.after !== undefined) q.push(["after", String(params.after)]);
    const resp = await this.request<M.TopLiveStreamsResponse>("GET", "/topLiveStreams", q);
    return resp?.topLiveStreams ?? {};
  }

  /**
   * Every LiveStream across all pages, fetched as the loop reaches them. `break` stops early;
   * `params.after` picks the starting point.
   */
  iterTopLiveStreams(params: TopLiveStreamsParams = {}): AsyncGenerator<M.LiveStream, void, undefined> {
    return paginate<TopLiveStreamsParams, M.LiveStream>(params, async (p) => {
      const page = await this.topLiveStreams(p);
      const nodes = (page.edges ?? []).flatMap((e) => (e?.node ? [e.node] : []));
      return [nodes, page.pageInfo?.hasNextPage ? page.pageInfo.endCursor ?? undefined : undefined];
    });
  }

  /**
   * The weekly trader leaderboard. Defaults to the current week. Returns one page;
   * `iterTraderLeaderboard` walks every page. `GET /traderLeaderboard`
   */
  async traderLeaderboard(params: TraderLeaderboardParams = {}): Promise<M.TraderLeaderboardEntryConnection> {
    const q: Array<[string, string]> = [];
    if (params.week !== undefined) q.push(["week", String(params.week)]);
    if (params.year !== undefined) q.push(["year", String(params.year)]);
    if (params.pageSize !== undefined) q.push(["first", String(params.pageSize)]);
    if (params.after !== undefined) q.push(["after", String(params.after)]);
    const resp = await this.request<M.TraderLeaderboardResponse>("GET", "/traderLeaderboard", q);
    return resp?.exploreTraderLeaderboard ?? {};
  }

  /**
   * Every TraderLeaderboardEntry across all pages, fetched as the loop reaches them. `break` stops
   * early; `params.after` picks the starting point.
   */
  iterTraderLeaderboard(params: TraderLeaderboardParams = {}): AsyncGenerator<M.TraderLeaderboardEntry, void, undefined> {
    return paginate<TraderLeaderboardParams, M.TraderLeaderboardEntry>(params, async (p) => {
      const page = await this.traderLeaderboard(p);
      const nodes = (page.edges ?? []).flatMap((e) => (e?.node ? [e.node] : []));
      return [nodes, page.pageInfo?.hasNextPage ? page.pageInfo.endCursor ?? undefined : undefined];
    });
  }

  /** A trend coin by its ticker. Resolves to `null` if there is no such coin. `GET /trendCoin` */
  async trendCoin(ticker: string, params: TrendCoinParams = {}): Promise<M.Zora20Token | null> {
    const q: Array<[string, string]> = [];
    q.push(["ticker", String(ticker)]);
    q.push(["chainId", String(params.chainId ?? BASE_CHAIN_ID)]);
    const resp = await this.request<M.TrendCoinResponse>("GET", "/trendCoin", q);
    return resp.trendCoin ?? null;
  }

  /**
   * Trend coins whose name matches, best match first. Returns one page; `iterTrendsByName` walks
   * every page. `GET /trendsByName`
   */
  async trendsByName(name: string, params: TrendsByNameParams = {}): Promise<M.Zora20TokenConnection> {
    const q: Array<[string, string]> = [];
    q.push(["name", String(name)]);
    if (params.pageSize !== undefined) q.push(["first", String(params.pageSize)]);
    if (params.after !== undefined) q.push(["after", String(params.after)]);
    const resp = await this.request<M.TrendsByNameResponse>("GET", "/trendsByName", q);
    return resp?.trendsByName ?? {};
  }

  /**
   * Every Zora20Token across all pages, fetched as the loop reaches them. `break` stops early;
   * `params.after` picks the starting point.
   */
  iterTrendsByName(name: string, params: TrendsByNameParams = {}): AsyncGenerator<M.Zora20Token, void, undefined> {
    return paginate<TrendsByNameParams, M.Zora20Token>(params, async (p) => {
      const page = await this.trendsByName(name, p);
      const nodes = (page.edges ?? []).flatMap((e) => (e?.node ? [e.node] : []));
      return [nodes, page.pageInfo?.hasNextPage ? page.pageInfo.endCursor ?? undefined : undefined];
    });
  }

  /**
   * Trades made by a wallet or profile, newest first. Returns one page; `iterWalletTradeActivity`
   * walks every page. `GET /walletTradeActivity`
   */
  async walletTradeActivity(identifier: string, params: WalletTradeActivityParams = {}): Promise<M.TradeActivityConnection> {
    const q: Array<[string, string]> = [];
    q.push(["identifier", String(identifier)]);
    if (params.after !== undefined) q.push(["after", String(params.after)]);
    if (params.pageSize !== undefined) q.push(["first", String(params.pageSize)]);
    const resp = await this.request<M.WalletTradeActivityResponse>("GET", "/walletTradeActivity", q);
    return resp?.walletAddressTradeActivity ?? {};
  }

  /**
   * Every TradeActivity across all pages, fetched as the loop reaches them. `break` stops early;
   * `params.after` picks the starting point.
   */
  iterWalletTradeActivity(identifier: string, params: WalletTradeActivityParams = {}): AsyncGenerator<M.TradeActivity, void, undefined> {
    return paginate<WalletTradeActivityParams, M.TradeActivity>(params, async (p) => {
      const page = await this.walletTradeActivity(identifier, p);
      const nodes = (page.edges ?? []).flatMap((e) => (e?.node ? [e.node] : []));
      return [nodes, page.pageInfo?.hasNextPage ? page.pageInfo.endCursor ?? undefined : undefined];
    });
  }

  /**
   * Quote a trade. The result holds the transaction that executes it. Amounts are in the input
   * token's smallest unit (wei for ETH). Set Referrer to earn the trade referral reward. Nothing
   * is signed or sent: pass the returned call to your wallet. `POST /quote`
   */
  async quote(req: M.QuoteRequest): Promise<M.QuoteResponse> {
    const q: Array<[string, string]> = [];
    const resp = await this.request<M.QuoteResponse>("POST", "/quote", q, req);
    const rawSuccess = (resp as { success?: unknown }).success;
    if (typeof rawSuccess === "string") resp.success = rawSuccess === "true";
    return resp;
  }

  /**
   * Build the transaction that creates a content coin. The result also holds the coin's predicted
   * address. Set PlatformReferrer to earn the platform referral share of the coin's trading fees
   * for its whole life. Nothing is signed or sent. `POST /create/content`
   */
  async createContentCoin(req: M.CreateContentCoinRequest): Promise<M.CreateContentCoinResponse> {
    const q: Array<[string, string]> = [];
    const resp = await this.request<M.CreateContentCoinResponse>("POST", "/create/content", q, req);
    return resp;
  }
}
